# Grafana datasource — auto-provisioned on container start.
# Connects to the Prometheus instance defined in docker-compose.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true