# Core
fastapi==0.141.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database
sqlalchemy==2.0.36
alembic==1.14.1
asyncpg==0.30.0
psycopg2-binary==2.9.10

# Auth
bcrypt==4.2.1
pyjwt[crypto]==2.13.0

# Storage
boto3==1.36.3

# Queue / Workers
celery[redis]==5.4.0
redis==5.2.1

# LLM (Phase 3 — tailored CV generation)
openai==2.53.0

# File validation
python-magic==0.4.27

# Document export (Sprint 5 — docx generation; PDF conversion is via the
# separate Gotenberg service, called over HTTP, no Python library needed)
docxtpl==0.19.0

# Malware scanning (clamd client)
clamd==1.0.2

# Observability
structlog==24.4.0
prometheus-client==0.21.1
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
# OTLP/HTTP protobuf export to Temps, plus request-level spans. The
# instrumentation packages version independently of the core SDK: 0.65b0 is
# the release built against 1.44.0, so these three move together.
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
# Error tracking. Temps is Sentry wire-compatible and injects SENTRY_DSN and
# SENTRY_RELEASE into every deployment, so there is no DSN to configure here.
sentry-sdk[fastapi]==2.68.1

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
factory-boy==3.3.1

# HTTP client (used by tests' TestClient, and at runtime by worker_export
# to call the Gotenberg PDF-conversion service)
httpx==0.28.1

# Email validation (required by Pydantic EmailStr)
email-validator==2.2.0

# Utilities
python-multipart==0.0.31
python-dotenv==1.2.2
